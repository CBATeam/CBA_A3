name = "CBA_A3"
prefix = "cba"
author = "CBATeam"
files = [
    "mod.cpp",
    "README.md",
    "LICENSE.md",
    "logo_cba_ca.paa",
    "meta.cpp"
    # "userconfig" # TODO in HEMTT
]

modname = "{{name}}"
key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip CBA_A3_v{{semver.major}}.{{semver.minor}}.{{semver.patch}}"
]

[header_exts]
version = "{{git \"id 8\"}}"
